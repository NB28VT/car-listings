<h1>Manufacturers</h1>

<table>
  <thead>
    <tr>
      <th width="200">Manufacturer</th>
      <th width="150">Country</th>
    </tr>
  </thead>
  <tbody>
    <% @manufacturers.each do |manufacturer| %>
      <tr>
        <td><%= manufacturer.name %></td>
        <td><%= manufacturer.country %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Add new manufacturer', new_manufacturer_path %>
